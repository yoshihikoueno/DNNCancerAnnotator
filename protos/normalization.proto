syntax = "proto2";

package netng.protos;

message BatchNormalization {
  optional float batch_norm_momentum = 1[default=0.99];
  optional float batch_norm_epsilon = 2[default=0.0001];
}

message InstanceNormalization {

}

message GroupNormalization {
  optional int32 groups = 1[default=32];
}