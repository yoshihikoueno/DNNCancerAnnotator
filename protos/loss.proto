syntax = "proto2";

package netng.protos;

message Loss {
  optional string name = 1[default=''];
  optional bool use_weighted = 2[default=false];
  optional float weight_constant_add = 3[default=0];
  optional float weight_constant_multiply = 4[default=1];
}